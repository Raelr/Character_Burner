// TODO: Create a single file for holding lifepaths (probably an array). 
// TODO: Allow lifepaths to be queried and found. 
const addLifePath = (name, setting, stock, time, leads, skills, traits) => {
    
}

// TODO: Append lifepath to array and save file. 
const saveLifePath = (path) => {

}

// TODO: Return lifepath array. 
const loadLifePath = (name) => {

}

module.exports = addLifePath